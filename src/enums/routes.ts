enum RoutesEnum {
  Home = "Home",
  Search = "Search",
  SearchResult = "SearchResult",
  Detail = "Detail",
  History = "History",
  CalculatingEmptyState = "CalculatingEmptyState",
}

export default RoutesEnum;
